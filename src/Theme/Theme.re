module Colors = {
  open Revery;
  let white = Color.hex("#fff");
  let lightGray = Color.hex("#cccccc");
  let background = Color.hex("#f8f9fc");
  let fontDark = Color.hex("#1d1d1d");
};

module Sizes = {
  let datesHeight = 75;

  let hoursWidth = 100;
  let hoursPaddingTop = 10;

  let hourHeight = 100;
  let hourFontSize = 14.;

  let minimumDayWidth = 200;
};